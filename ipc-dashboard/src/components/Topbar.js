// src/components/Topbar.js
import React from "react";

export default function Topbar({ title }) {
  return (
    <div style={{ marginBottom: 10 }}>
      {/* The page's topbar is rendered inside page components; keep this for compatibility */}
    </div>
  );
}
